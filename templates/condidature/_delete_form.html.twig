<form method="post" action="{{ path('condidature_delete', {'id': condidature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ condidature.id) }}">
    <button class="btn">Delete</button>
</form>
