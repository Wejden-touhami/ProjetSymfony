<form method="post" action="{{ path('confirmation_delete', {'id': confirmation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirmation.id) }}">
    <button class="btn">Delete</button>
</form>
