{% extends 'base.html.twig' %}

{% block title %}Confirmation{% endblock %}

{% block body %}
    <h1>Confirmation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ confirmation.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('confirmation_index') }}">back to list</a>

    <a href="{{ path('confirmation_edit', {'id': confirmation.id}) }}">edit</a>

    {{ include('confirmation/_delete_form.html.twig') }}
{% endblock %}
